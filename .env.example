# YouTube Data API v3
# Create a server-side API key in Google Cloud and add the same values in Vercel.
YOUTUBE_API_KEY=
YOUTUBE_CHANNEL_ID=

# Optional: if you already know the channel's uploads playlist ID, set it here.
# When present, the site skips the channels.list lookup and reads uploads directly.
YOUTUBE_UPLOADS_PLAYLIST_ID=
